#ifndef MY_MATH_H
#define MY_MATH_H

/* 최대공약수(GCD)를 반환한다.
   인자 a, b는 정수이며 양·음수 모두 처리한다. */
int gcd(int a, int b);

/* 최소공배수(LCM)를 반환한다.
   a 또는 b가 0이면 0을 반환한다. */
int lcm(int a, int b);

#endif // MY_MATH_H